---
status: resolved
blocked-by: [09, 11, 12]
---

# feat(site): serve the site under the /saud-alnasser/ base path of the project site

## Outcome
The build is addressed at `https://saud-alnasser.github.io/saud-alnasser/`: `astro.config.mjs` carries the base, one module joins paths to it, and every address the site publishes carries it, in the pages, the downloads, the root redirect, the sitemaps, `robots.txt`, and the JSON Resume documents. The local server, the PDF render, the browser tests, the dist checks, and Lighthouse all run under the same base, so a path written without it fails here before it reaches Pages.

## Acceptance Criteria
- [x] After `pnpm build`, every internal `href` in `dist/**/*.html`, the meta-refresh target of `dist/index.html`, the `<link rel="sitemap">`, the canonical, `hreflang`, and `og:url` addresses, the CV page's two download links, every `<loc>` in the sitemaps, the `Sitemap:` line of `robots.txt`, and `meta.canonical` and `basics.url` in both `resume.json` documents start with `/saud-alnasser/` or `https://saud-alnasser.github.io/saud-alnasser/` (criterion 8, criterion 10, criterion 15).
  Verified 2026-09-09 after `pnpm build` in the run's surface: `dist/index.html` carries `content="0;url=/saud-alnasser/en/"`; the hrefs of `dist/en/index.html` are `/saud-alnasser/ar/`, `/saud-alnasser/en/`, `/saud-alnasser/en/cv/`, `/saud-alnasser/en/education/`, `/saud-alnasser/en/work/`, `/saud-alnasser/_astro/Base.HEhomE8V.css`, and `/saud-alnasser/sitemap-index.xml`, with the canonical, `hreflang`, and `og:url` addresses at `https://saud-alnasser.github.io/saud-alnasser/en/` and `/ar/`; the CV page links `/saud-alnasser/cv.en.pdf` and `/saud-alnasser/en/resume.json`; `sitemap-0.xml` lists the eight pages under `https://saud-alnasser.github.io/saud-alnasser/`; `dist/robots.txt` reads `Sitemap: https://saud-alnasser.github.io/saud-alnasser/sitemap-index.xml`; `ar/resume.json` carries `basics.url` `https://saud-alnasser.github.io/saud-alnasser/` and `meta.canonical` `https://saud-alnasser.github.io/saud-alnasser/ar/resume.json`.
- [x] No file under `src/` writes a leading-slash site path literal: every path goes through `src/lib/paths.ts`, and `scripts/check-dist.mjs` fails by name when a page carries an internal link, a sitemap entry, a `robots.txt` sitemap line, or a `resume.json` address without the base (criterion 15, requirement 9).
  Verified: a grep of `src/` for a quoted leading-slash site path finds only the two arguments handed to `withBase` (`/sitemap-index.xml` in `Base.astro`, `/cv.<locale>.pdf` in `cv.astro`); `robots.txt` moved from `public/` to the endpoint `src/pages/robots.txt.ts` so its sitemap line is derived as well. `pnpm check:dist` prints `base paths: 76 root-relative paths in 10 files, all under /saud-alnasser/`; with one navigation link in `dist/en/index.html` rewritten to `/en/work/` it fails with `check-dist: base paths: dist/en/index.html refers to /en/work/, which is outside the site's base path /saud-alnasser/`, and with the base stripped from the sitemap line of `dist/robots.txt` it fails with `check-dist: robots: dist/robots.txt has no line "Sitemap: https://saud-alnasser.github.io/saud-alnasser/sitemap-index.xml"`. The `json resume` check asserts `basics.url` and `meta.canonical` against the same root, and the `sitemap` check refuses an entry outside it.
- [x] `scripts/serve-dist.mjs` serves `dist/` under the base and answers 404 at `/en/`; `pnpm render:pdf`, `pnpm check:dist`, `pnpm test`, `pnpm test:content`, and `pnpm lighthouse` pass against it with the same results as before (criterion 5, criterion 6, criterion 7, criterion 13).
  Verified: with `node scripts/serve-dist.mjs 4199`, `curl` answered 404 for `/en/`, 301 to `/saud-alnasser/` for `/saud-alnasser`, and 200 for `/saud-alnasser/en/`, the stylesheet, and `/saud-alnasser/cv.en.pdf`. `pnpm render:pdf` wrote `cv.en.pdf` (72836 bytes) and `cv.ar.pdf` (119552 bytes, both Noto Naskh Arabic weights loaded); `pnpm check:dist` exited 0 with 12 checks; `pnpm test` printed `74 passed (9.0s)`, the pages now named `/saud-alnasser/en/` and so on; `pnpm test:content` printed `passed`. `pnpm lighthouse`, now `scripts/lighthouse.mjs`, printed the four addresses under the base and `Healthcheck passed!`, then died in chrome-launcher's temp-profile cleanup (`EPERM`), the Windows failure the run log already records; Lighthouse 12.6.1 run directly on the same four addresses with a kept profile directory scored, mobile: `/en/` and `/en/cv/` performance 1, accessibility 1, best practices 0.96; `/ar/` and `/ar/cv/` performance 0.92, accessibility 1, best practices 0.96. The Linux runner stays the authoritative Lighthouse gate.
- [x] With `base` set to `/`, the build and `pnpm check:dist` still pass, so a custom domain added later needs one change and nothing else (requirement 8).
  Verified: with `export const base = '/'` in `astro.config.mjs` and nothing else changed, `pnpm build` built 8 pages, `dist/index.html` refreshed to `/en/`, `dist/robots.txt` named `https://saud-alnasser.github.io/sitemap-index.xml`, and `pnpm check:dist` exited 0 with `base paths: 76 root-relative paths in 10 files, all under /`; `base` was set back and the build repeated. Before `robots.txt` became an endpoint the same probe failed on its hand-written sitemap line, which is why it moved.

## Relevant areas
`astro.config.mjs`, `src/lib/paths.ts` (new), `src/pages/robots.txt.ts` (new, replacing `public/robots.txt`), `src/layouts/Base.astro`, `src/pages/[locale]/index.astro`, `src/pages/[locale]/cv.astro`, `src/lib/resume.ts`, `scripts/serve-dist.mjs`, `scripts/render-pdf.mjs`, `scripts/check-dist.mjs`, `scripts/lighthouse.mjs` (new), `tests/pages.ts`, `tests/*.spec.ts`, `playwright.config.ts`, `lighthouserc.json`.

## Constraints
- One constant: `base` in `astro.config.mjs`. The tests and the scripts read `site` and `base` from that file; nothing repeats the string.
- The plan's "The address" section says what Astro prefixes and what it does not; rely on nothing beyond that.
- No change to the content, the contract, or the deploy workflow.

## Notes
Appended on 2026-09-09 by the return to plan after Saud kept the repository name `saud-alnasser`. The probe build in [[efforts/1-portfolio-site/evidence/research/repository-name-and-base-path]] (F5 to F9) is the list of what breaks under the base.

Built by the orchestrator on 2026-09-09 as a wave of one. `public/robots.txt` became the endpoint `src/pages/robots.txt.ts`, because a static file could not follow `base` and the fourth criterion asks for one change. Lighthouse's `staticDistDir` can only serve at the root, so `pnpm lighthouse` is now a script that starts the site's own server and hands the addresses to `lhci autorun`; `lighthouserc.json` keeps the thresholds and the run count. Recorded, not acted on: a crawler reads `robots.txt` at the root of the host, `https://saud-alnasser.github.io/robots.txt`, which no repository serves, so the file under the base is not where a crawler looks for it; indexing is permitted by the absence of a root file, and every page announces the sitemap with `<link rel="sitemap">`. A search console submission of `https://saud-alnasser.github.io/saud-alnasser/sitemap.xml` is Saud's to make if he wants the pages indexed sooner.
