---
status: resolved
blocked-by: [01]
---

# feat(site): experience before projects, courses and certifications as their own grids, and the home page's section index

## Outcome
The work page shows the experience grid first and the projects grid after it, each under a heading with an id. The education page shows the studies timeline, whose online-courses node counts the courses and links to `#courses`, then the courses grid, then the certifications grid, with the one dialog serving both. The home page shows one section card per section, experience, projects, education, courses, certifications, and skills, each linking to its section's heading and counting what it renders, beside the CV card it has today; the resume card and the resume link in the navigation arrive with ticket 03. Every new string exists in both languages and the browser tests assert the new shape.

## Acceptance Criteria
- [x] On both work pages the experience grid precedes the projects grid in document order, each under its own heading; `tests/work.spec.ts` asserts the order and passes (criterion 4). Verified 2026-09-10: the new test reads the positions of `h2#experience`, `[data-grid="experience"]`, `h2#projects`, and `[data-grid="projects"]` out of the document order of `main`; `✓ tests\work.spec.ts:108:5 › ... /en/work/ puts the experience grid before the projects grid` and the Arabic twin, the spec 34 passed. Proven to fire: with the sections swapped back, all four projections failed with `Error: the experience grid comes before the projects heading on /saud-alnasser/en/work/ Expected: < 3 Received: 238`; page restored, 34 passed.
- [x] On both education pages `[data-grid="courses"]` holds one card per `kind: course` entry in date order with undated ones last, each showing its name, its issuer, and its date where known, and each card naming a document opens the dialog with the redesign's close, link, Escape, and focus behaviour; `[data-grid="certifications"]` holds one card per `kind: certification` entry with the same behaviour; `tests/certificates.spec.ts` asserts both grids and passes (criterion 5, criterion 6). Verified 2026-09-10: the spec runs a row per grid asserting the card count, the set of names against the entries of that kind, every card's `data-entry`, the sequence of dates against `byDateAscending` so undated ones land last, and the issuer and formatted date on each card; the dialog set (open with preview and PDF, Escape, close control, focus returned) runs once per grid, so the one certification exercises it too. `✓ ... shows every courses entry as a card, with its issuer and its date` and the certifications twin in both languages; the spec 58 passed, up from 40. `Certificate.astro` carries `data-entry={data.kind}` and the dialog script keys on `[data-document]`, so one dialog serves both grids unchanged.
- [x] The timeline's online-courses node shows the count of the courses grid, worded with the courses noun, and links to `#courses`; `tests/education.spec.ts` asserts it and passes (criterion 5). Verified 2026-09-10: the node's count and period derive from the courses rather than from every certificate, and the spec asserts the name, the count line built from `courses.length` and `t.education.onlineCourses.noun`, the period, the link text, and `href="#courses"`; `✓ ... counts the courses on the node and dates it from the first to the last` and `✓ ... lists the courses under the heading the node leads to, and the certifications under their own` in both languages, the spec 28 passed. The node's dated period is unchanged in value, because the one certification's date sits between the first and last course dates.
- [x] Reclassifying one entry's `kind` and rebuilding moves its card from one grid to the other with no other edit, tried once and recorded (criterion 6). Verified 2026-09-10: `sololearn-html.yaml` changed from `kind: course` to `kind: certification` and rebuilt; the courses grid went 26 to 25 cards and the certifications grid 1 to 2, the card moving with it, and in the same build the home page's counts read `25 courses` and `2 certifications` and the timeline node `25 courses`, with no edit to any page. Reverted with `git checkout --`; the rebuild returned 26 and 1, and the content file is unmodified in the commit.
- [x] On both home pages the section grid shows seven cards in order, experience, projects, education, courses, certifications, skills, CV, each section card linking to its section's heading anchor and showing a count equal to the entries that section renders; `tests/home.spec.ts` asserts the cards, their anchors, and their counts from the collections through the predicate, and passes (criterion 4, criterion 7). Verified 2026-09-10: the built English page carries, in order, `/en/work/#experience`, `/en/work/#projects`, `/en/education/#studies`, `/en/education/#courses`, `/en/education/#certificates`, `/en/#skills`, and `/en/cv/`, with counts `1 position`, `13 projects`, `1 institution`, `26 courses`, `1 certification`, `6 skill groups` and no `data-counts` on the CV card; Arabic the same in its own plural forms. The spec computes every count from `src/content/` through `isShown`, `isCourse`, and `isCertification`, and its third test follows each anchor and asserts a heading with that id exists to land on; `✓ shows one card per section, in the order the site reads in`, `✓ leads each section card to its heading and counts what it holds`, `✓ lands each section card on the heading it names` in both languages, the spec 48 passed.
- [x] At 360 pixels every grid on the work, education, and home pages is one column with no horizontal scroll, and at 1440 two or more, asserted by the per-page tests as today (criterion 7). Verified 2026-09-10: the education spec's width tests now loop over both grids; `✓ lays both grids in one column and does not scroll sideways`, `✓ lays both grids in two or more columns`, `✓ .../work/ lays one column and does not scroll sideways`, `✓ .../work/ lays two or more columns, none wider than its track`, `✓ the section grid is one column` and `✓ the section grid is two or more columns` on both home pages, and `✓ layout.spec.ts › at 360 pixels wide › every page does not scroll horizontally`.
- [x] Every new string exists in both languages in `src/lib/i18n.ts`, `pnpm check` passes, and the dist check's gap line does not grow; `tests/contrast.spec.ts`, `tests/layout.spec.ts`, and `tests/menu.spec.ts` pass over every page including the no-script context (criterion 14). Verified 2026-09-10: `sections.courses`, `sections.certifications`, `home.experience`, `home.projects`, `home.courses`, `home.certifications`, `home.skills`, and the `home.counts.nouns` entries for courses, certifications, and skills exist in both languages, the counted nouns in the six forms Arabic distinguishes, and `education.onlineCourses.noun` and `.link` reworded to courses; the `Strings` type is derived from the English object, so a key missing from `ar` is a type error, and `pnpm check` printed `Result (49 files): 0 errors`. The gap line read `[localized] 0 gaps` before and after. `contrast.spec.ts` 32 passed, `layout.spec.ts` 50 passed, `menu.spec.ts` 46 passed, its no-script context asserting each of the eight pages' `main` text and language list.

## Relevant areas
`src/pages/[locale]/work/index.astro`, `src/pages/[locale]/education/index.astro`, `src/pages/[locale]/index.astro`, `src/components/Certificate.astro` (`data-entry` from `kind`), `src/components/SectionCard.astro`, `src/lib/i18n.ts`, `tests/work.spec.ts`, `tests/education.spec.ts`, `tests/certificates.spec.ts`, `tests/home.spec.ts`.

## Constraints
- The routes are the plan's option B, chosen by Saud: no route is added or removed here; sections are headings with ids on the pages that hold them, and the home cards link to those anchors. The `section` attribute values and the `data-grid` and `data-entry` values are the plan's "Interfaces".
- The certifications grid keeps the `#certificates` id on its heading so an inbound link still lands; the courses grid takes `#courses`.
- The skills card links to `#skills` on the home page and counts the skill groups.
- The strings this ticket adds are the plan's "Components" row for `i18n.ts`, minus the resume and document strings, which ticket 03 adds; the two sets share no key.

## Notes
The plan's technical approach step 2. Stacks on 01; independent of 03 and 04 except that 03 also edits `index.astro` and `i18n.ts`, which the orchestrator reconciles at integration.

The education card's anchor is `#studies`, the education section's own heading on that page, which the ticket's list left unstated while naming `#courses` and `#certificates`. It satisfies the plan's rule that a card takes its section's heading anchor and the spec's criterion 7, and it keeps the six section cards consistent; recorded here rather than decided silently.

`src/lib/i18n.ts` lost three keys this change orphaned, `home.work`, `home.counts.nouns.certificates`, and `sections.certificates`, which is the one non-additive part of that file and the seam ticket 03 meets.
