---
status: resolved
---

# feat(content): a completion status on projects, a kind on certificates, a resume marker, and the one predicate every output reads

## Outcome
The content contract carries the three fields the plan's "Interfaces" names, the content is authored to the plan's "Data model", and `src/lib/shown.ts` is the one place that decides whether a project appears. Every output that filters projects reads it: the pages, the JSON Resume mapper, the dist check, the content mechanism test, and the browser tests that compute their expectations from `src/content/`. Only finished work is shown, screeps is in no output, and the content README documents the fields. The site still has today's sections; splitting and reordering them is ticket 02, and the documents are ticket 03.

## Acceptance Criteria
- [x] Every project file carries `status`, every certificate file carries `kind`, and `pnpm build` refuses a project without `status` and a certificate without `kind`, naming the file, tried once with each field removed (criterion 1, criterion 15). Verified 2026-09-10: 19 of 19 project files carry `status`, 27 of 27 certificate files carry `kind`; with `status` deleted from `mudaraj.yaml`, `pnpm build` exited non-zero printing `[InvalidContentEntryDataError] projects → mudaraj data does not match collection schema. status: Invalid option: expected one of "completed"|"in-progress"` and the file's path; with `kind` deleted from `sololearn-html.yaml`, the same for `certificates → sololearn-html ... kind: Invalid option: expected one of "course"|"certification"`; both files restored.
- [x] Setting one project's status to `in-progress` and building removes it from the work page, the home page's project count, the CV page, and both JSON Resume documents with no other edit; a search of `dist/` for its name finds nothing (criterion 1). Verified 2026-09-10: `mudaraj.yaml` set to `status: in-progress`, `pnpm build` green, `grep -rl Mudaraj dist/` printed nothing, both JSON documents carried 12 projects where the unchanged build carries 13; restored.
- [x] A search of `dist/` and both JSON Resume documents for "screeps" finds nothing; the file stays in `src/content/projects/` with `visibility: hidden` and `status: in-progress` (criterion 2). Verified 2026-09-10: `grep -ril screeps dist/` printed nothing after the build and the render step; `screeps.yaml` ends `visibility: hidden` / `status: in-progress`. The same search for "Nova", "nexuscord", "ETG", "discord-trengo-integration", and "AEP" also printed nothing.
- [x] `pnpm check:dist` passes with `jsonResume` expecting the set `isShown` admits, computed by parsing each project file rather than matching `visibility: hidden`, and the JSON documents carry exactly that set (criterion 12). Verified 2026-09-10: `visibleEntries` parses each file with `yaml` and filters projects through `isShown`; `pnpm check:dist` exited 0 printing `en/resume.json: valid, work 1, education 1, certificates 27, skills 6, projects 13` and the same for `ar`, `cv.en.pdf: 77317 bytes, 8 expected facts found in 6 groups in reading order`, `[localized] 0 gaps`, and the README check passing; the 13 are rentable, Mudaraj, CourseViewer, PL/0 compiler, Personal information form, leetcode, CPU scheduling simulator, godot-brackeys-simple-platformer, advent-of-code, bevy-pong, monkey-lang, learning-rust, cachescribe.
- [x] `tests/home.spec.ts` and `tests/work.spec.ts` compute their project expectations through `isShown` and pass against the built site; `pnpm test` passes (criterion 1). Verified 2026-09-10: both tests import `isShown` from `src/lib/shown.ts` and parse each project file with `yaml`; no regular expression over `visibility: hidden` remains in `scripts/` or `tests/`; `pnpm test` printed `272 passed (19.3s)` over both palettes.
- [x] `pnpm test:content` passes with the fixture project carrying `status: completed` and `resume: true` and the fixture certificate carrying `kind: course`; the fixture project is asserted in the work page, the CV page, and the JSON document in both languages, six files until ticket 03 adds the resume page (criterion 15). Verified 2026-09-10: with pnpm's `NODE_PATH` passed through, as the redesign's ticket recorded, the script printed `present: "fixture-mechanism-probe-4f9c2e" in dist/en/work/index.html, dist/ar/work/index.html, dist/en/cv/index.html, dist/ar/cv/index.html, dist/en/resume.json, dist/ar/resume.json and nowhere else`, the same six for the certificate on the education page, `unopenable` for both languages, `absent` for both after removal, and `test-content-mechanism: passed`; no fixture left in the tree. Under Git Bash the sh shim writes `NODE_PATH` in `/proc/cygdrive` form, which Node does not read, and the fixture build fails with `Tsconfig not found astro/tsconfigs/strict` before any content is read; an environment note, not a defect of the change.
- [x] `src/content/README.md` documents `status` and its values, `kind` and its values, and `resume`, and says which output each affects (criterion 15). Verified 2026-09-10: the `projects/` table carries `status` (`completed`, `in-progress`; an `in-progress` project appears in no output, the work page, the home count, the CV, the resume, and the JSON document named) and `resume` (the one-page resume; the CV unaffected), with a paragraph naming `src/lib/shown.ts` as the one place; the `certificates/` section explains course against certification, its table carries `kind` and `resume`; the step-by-step gains the status and the marker.

## Relevant areas
`src/content.config.ts`, new `src/lib/shown.ts`, `src/lib/resume.ts`, `src/pages/[locale]/index.astro`, `src/pages/[locale]/work/index.astro`, `src/pages/[locale]/cv.astro`, `src/content/projects/*.yaml`, `src/content/certificates/*.yaml`, `scripts/check-dist.mjs` (`visibleEntries`, `jsonResume`, `gaps`), `scripts/test-content-mechanism.mjs`, `tests/home.spec.ts`, `tests/work.spec.ts`, `src/content/README.md`.

## Constraints
- The field names, values, and the predicate's signature are the plan's "Interfaces"; the per-project statuses, the resume marks, and the kinds are the plan's "Data model", which is the spec's confirmed table. Author them exactly; a project not in the table does not exist.
- `status` is required on projects; `kind` is required on certificates; `resume` is optional and absent means false. The JSON Resume mapper does not emit `resume` or `kind`.
- The scripts and tests import `src/lib/shown.ts` the way they import `src/lib/order.ts` today, with Node stripping the types; no regular expression over the YAML remains for a project's visibility.
- The content mechanism test's outputs list for the project grows to eight in ticket 03; here it stays at the six that exist, and the resume flag on the fixture is authored now so ticket 03 changes the list alone.

## Notes
The plan's technical approach step 1. Every other ticket stacks on this branch.
